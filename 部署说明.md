# 婚礼 H5 电子请柬 · GitHub Pages 部署说明

## 文件清单

| 文件 | 说明 |
|---|---|
| `index.html` | 请柬主文件（6 张照片已内嵌，约 2.4MB） |
| `music.mp3` | 背景音乐（大花轿，约 9.4MB） |
| `confetti.min.js` | 彩带特效库（本地文件，避免 CDN 失效） |
| `.nojekyll` | 空文件，告诉 GitHub 原样发布、不要处理（必须一起传） |

> 四个文件必须放在**同一个目录**，不要只传 index.html。

---

## 一、注册 GitHub 账号（已有账号跳过）

1. 打开 https://github.com ，点 **Sign up**，用邮箱注册（QQ/163 邮箱均可）。
2. 注册时如果验证页面加载慢，可换网络或多试几次；
   **看请柬的亲友不需要翻墙**，只有你自己上传时需要能打开 github.com。

## 二、创建仓库

1. 登录后点右上角 **+** → **New repository**。
2. Repository name 填：`wedding`（小写，最终链接为 `你的用户名.github.io/wedding/`）。
3. 选择 **Public（公开）**——Private 仓库无法免费开启 Pages。
4. 勾选 **Add a README file**（勾不勾都可以，勾了更省事）。
5. 点 **Create repository**。

## 三、上传文件（网页直传，不用装软件）

1. 进入刚建好的仓库，点 **Add file** → **Upload files**。
2. 把上面 4 个文件（含 `.nojekyll`）**一起拖进网页**，
   等进度条全部上传完成（音乐 9.4MB，约 1–3 分钟）。
3. 页面底部 Commit message 填 `婚礼请柬`，点 **Commit changes**。

> 若上传中途失败：删掉半成品重新传，或换个网络再试。

## 四、开启 GitHub Pages

1. 仓库顶部点 **Settings**（设置）→ 左侧菜单 **Pages**。
2. **Source** 下拉选 **Deploy from a branch**。
3. 下面两个框分别选 **main** 和 **/ (root)**，点 **Save**。
4. 等 1–2 分钟并刷新该页面，顶部会出现链接：
   `https://你的用户名.github.io/wedding/`
   点开就是请柬，把这个链接发到微信即可。

---

## 五、开通 CloudBase 实时弹幕与回执（约 15 分钟）

**不做这一步，页面也能正常浏览**，但弹幕只在本机演示（表单下方会有提示）。

1. 打开腾讯云 **云开发 CloudBase** 控制台 → 新建环境，选 **按量计费**
   （小流量在免费额度内），创建后复制 **环境 ID**（形如 `wedding-1a2b3c4d`）。
2. 「登录授权 → 登录方式」开启 **匿名登录**。
3. 「数据库」新建集合，名称必须是 **`wedding_rsvp`**。
4. 该集合的「权限设置」选 **自定义安全规则**，粘贴：

   ```json
   {
     "read": true,
     "write": "doc._openid == auth.openid"
   }
   ```

   含义：所有人可读（弹幕可见），每人只能写自己的记录。
5. 「环境 → 安全配置 → Web 安全域名」中，添加你的域名：
   **`你的用户名.github.io`**（只填域名，不带 https、不带路径）。
   **不加这一步，手机上提交回执会失败。**
6. 用记事本打开 `index.html`，找到顶部配置区，填入环境 ID：

   ```js
   const CONFIG = {
     cloudbaseEnvId: 'wedding-1a2b3c4d',   // ← 填这里
   ```

7. 回到 GitHub 仓库，重新上传 `index.html`（提示覆盖时确认），
   等 1–2 分钟 Pages 自动更新。

### 查看 / 导出回执

CloudBase 控制台 → 数据库 → `wedding_rsvp` 集合，可看到每条
姓名、是否出席、人数、忌口、祝福、时间；点「导出」可下载表格。
不合适的弹幕直接在控制台删除，页面上会同步消失。

---

## 六、发布前自测清单

- [ ] 用**手机流量（4G/5G）**在微信里打开链接，不要只连 WiFi 测
- [ ] 点击「点击进入请柬」，音乐响起、花瓣飘落、彩带散开
- [ ] 上滑逐页浏览，封面自动轮播，相册可左右滑动
- [ ] 倒计时正常；「复制地址」提示成功；腾讯/高德按钮能打开地图
- [ ] 提交一条回执，看到彩带 + 弹幕；**换另一台手机**打开也能看到
- [ ] 发给 3–5 位不同运营商（移动/联通/电信）的亲友，确认都能顺畅打开

> GitHub Pages 国内访问偶尔偏慢。如果实测多人反馈打不开，
> 告诉我，可改走 EdgeOne 或备案域名方案，页面文件不用重做。

---

## 七、以后想改内容

- 改文字、流程时间、地图链接：编辑 `index.html` 顶部 `CONFIG` 或页面中文，
  重新上传覆盖即可；GitHub Pages 有几分钟缓存，手机上可在链接后加 `?v=2` 强制刷新。
- 换音乐：替换同名 `music.mp3` 后重新上传。
- 换照片：照片是内嵌的，把新照片发我重新生成文件即可。
- 微信对音频有自动播放限制，首屏必须点一下「点击进入请柬」才会响，
  这是微信的通用限制，不是 bug。
